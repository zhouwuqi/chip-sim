{
  "name": "chipsim",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "2D logic-circuit sandbox — grid placement, netlist kernel, synchronous delta-step simulation",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 4000",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4000",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  }
}
